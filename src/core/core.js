const PagesUrls = {
  HOME: "/",
  RESTAURANTS: "/restaurants",
  AUTH: "/auth",
  LOGIN: "/auth/login",
};

const TranslationFiles = {
  COMMON: "common",
  HOME: "home",
  RESTAURANTS: "restaurants",
};

const TimeInSeconds = {
  DAY: 60 * 60 * 24,
  HOUR: 60 * 60,
  MINUTE: 60,
};

module.exports = {
  PagesUrls,
  TranslationFiles,
  TimeInSeconds,
};
